# Pinned, not floated. The SDK is what launches Chromium inside the sandbox,
# and `>=0.110.0` resolved to 0.129.0 here and 0.214.0 on the host -- eighty-five
# versions apart. Every page then failed with "Chromium never opened its debug
# port", on the host only, on a site that audited cleanly from a laptop.
# A version range is not a dependency; it is a difference between machines.
daytona==0.129.0

anthropic>=0.75.0
psycopg2-binary>=2.9.9
certifi>=2024.2.2
